# Copy this file to `.env` (gitignored) and fill in the real values.
# DO NOT commit your real service role key to source control.

# Supabase project URL (your project's URL, e.g. https://xxxx.supabase.co)
SUPABASE_URL=

# Supabase service role key (server-side only). Required for server uploads and admin operations.
SUPABASE_SERVICE_ROLE_KEY=

# Optional anon key fallback (only for non-admin server operations)
SUPABASE_ANON_KEY=

# Optional admin token used by some dev-only endpoints
ADMIN_TOKEN=

# When running in production set NODE_ENV=production
NODE_ENV=development
