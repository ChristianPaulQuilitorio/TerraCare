# Copy this file to .env and set your values
# Supabase project URL (https://<ref>.supabase.co)
SUPABASE_URL=
# Supabase anon key (browser key) used as fallback if no service role key is set (not recommended for writes)
SUPABASE_ANON_KEY=
# Supabase service role key (NEVER expose to browser)
SUPABASE_SERVICE_ROLE_KEY=
# Optional: server port for SSR Express
PORT=4000
