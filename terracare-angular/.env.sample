## Copy this file to .env (never commit the real .env)

# Public values used by the Angular app (written into environment.production.ts at build time)
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Optional: Server-only value for serverless/API usage (NEVER expose to browser bundles)
# SUPABASE_SERVICE_ROLE_KEY=

# Optional: Base URL for APIs (leave blank for same-origin)
API_BASE=

# Notes:
# - On Vercel, set these in Project Settings > Environment Variables.
# - The build script scripts/generate-env.mjs reads SUPABASE_URL and SUPABASE_ANON_KEY
#   and writes them into src/environments/environment.production.ts.
