## Copy this file to .env (never commit the real .env)
## These variables are loaded ONLY on the server runtime (SSR / API routes) via dotenv in server.ts

# Base URL of your public API that the Angular browser app will call.
# For purely internal (same origin) API served by server.ts, you can leave this blank.
API_BASE=http://localhost:4000

# Supabase project URL (format: https://<ref>.supabase.co)
SUPABASE_URL=https://tyalzymvdnrzbwwtlcvj.supabase.co

# Supabase anon (public) key. Safe for browser usage but still keep it out of git.
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InR5YWx6eW12ZG5yemJ3d3RsY3ZqIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTk0MjAzODAsImV4cCI6MjA3NDk5NjM4MH0.gP0SnNZOQ3H6mmIQ0NvlPwW3TMUe7nvzKpFyAa4TPaM

# Supabase service role key (NEVER expose to browser bundles). Used only on the Node server for privileged operations.
SUPABASE_SERVICE_ROLE_KEY=

# Optional: Override the Express server port (default 4000 for local dev)
PORT=4000

# NOTES:
# 1. On Vercel: set these in Project Settings > Environment Variables instead of using a .env file.
# 2. Do NOT include SUPABASE_SERVICE_ROLE_KEY in any generated environment.*.ts file (keep server-only).
# 3. If you add a build-time generator (scripts/generate-env.mjs) it should read only the public values
#    (API_BASE, SUPABASE_URL, SUPABASE_ANON_KEY) and write them into environment.production.ts.
