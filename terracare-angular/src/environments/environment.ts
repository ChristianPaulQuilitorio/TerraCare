export const environment = {
  production: false,
  apiBase: '', // use relative /api calls in dev; or set to 'http://localhost:4000' if running local API
  supabaseUrl: '', // set locally via .env + generator or manually for local only
  supabaseAnonKey: '' // DO NOT commit real anon key
};
