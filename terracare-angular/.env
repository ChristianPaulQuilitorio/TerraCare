# Supabase server config
SUPABASE_URL=https://tyalzymvdnrzbwwtlcvj.supabase.co
# Note: For write operations from server API, set the service role key here.
# If you don't set it, GET endpoints that use anon key may still work if RLS allows.
SUPABASE_SERVICE_ROLE_KEY=
PORT=4000
